imports:
    - { resource: config.yml }

monolog:
    handlers:
        main:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: debug

#swiftmailer:
#    delivery_address: me@example.com
